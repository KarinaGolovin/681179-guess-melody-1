import React from 'react';
import PropTypes from 'prop-types';

const AudioPlayer = (props) => {
  const {trackSrc, } = props;

  return (
    <audio>

    </audio>
  );
};

AudioPlayer.propTypes = {

};

export default AudioPlayer;
